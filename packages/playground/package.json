{
    "name": "@cosmos/playground",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "rspack --config ../../rspack.playground.config.js --mode=production --node-env=production",
        "serve": "rspack serve --config ../../rspack.playground.config.js"
    }
}
