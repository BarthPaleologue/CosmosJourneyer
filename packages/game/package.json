{
    "name": "@cosmos/game",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "rspack --config ../../rspack.game.config.js --mode=production --node-env=production",
        "serve": "rspack serve --config ../../rspack.game.config.js"
    }
}
